name: azure-semantic-kernel-mcp
metadata:
  template: azure-semantic-kernel-mcp
services:
  weather-mcp-server: 
    project: ./src/agent
    host: containerapp
    language: python
    docker:
      path: Dockerfile
      context: .
      remoteBuild: true
  agent-mcp-server:
    project: ./src/weather
    host: containerapp
    language: python
    docker:
      path: Dockerfile
      context: .
      remoteBuild: true
